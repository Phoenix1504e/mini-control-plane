# Results

Place structured experiment outputs here as JSON Lines files. Each line should include at least:

```json
{"run":"table1-n2","controller":"controller-0","resource":"app-demo","event":"conflict"}
```

Do not commit local runtime files such as `leader.lock` or unstructured `events.log` output.
